apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datafoldapplications.datafold.datafold.com
spec:
  group: datafold.datafold.com
  names:
    kind: DatafoldApplication
    listKind: DatafoldApplicationList
    plural: datafoldapplications
    singular: datafoldapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatafoldApplication is the Schema for the datafoldapplications
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatafoldApplicationSpec defines the desired state of DatafoldApplication.
            properties:
              clickhouse:
                description: Clickhouse contains configuration for ClickHouse database
                properties:
                  database:
                    default: clickhouse
                    description: Database specifies the database name
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      ClickHouse password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  port:
                    default: 9000
                    description: Port specifies the port for ClickHouse server
                    format: int32
                    type: integer
                  user:
                    default: default
                    description: User specifies the ClickHouse user
                    type: string
                type: object
              components:
                description: Components specifies which components to install and
                  their configuration
                properties:
                  clickhouse:
                    description: ClickHouse enables ClickHouse database
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  dfshell:
                    description: Dfshell enables the dfshell component
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  dma:
                    description: Dma enables the DMA component
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  dma2:
                    description: Dma2 enables the second DMA instance
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  dma3:
                    description: Dma3 enables the third DMA instance
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  kept:
                    description: Kept enables the graph server
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  nginx:
                    description: Nginx enables the nginx ingress
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  operator:
                    description: Operator enables the operator component
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  redis:
                    description: Redis enables Redis cache
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  scheduler:
                    description: Scheduler enables the scheduler component
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  server:
                    description: Server enables the main Datafold server
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  storage-worker:
                    description: StorageWorker enables the storage worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker:
                    description: Worker enables the worker components
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-catalog:
                    description: WorkerCatalog enables the catalog worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-interactive:
                    description: WorkerInteractive enables the interactive worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-lineage:
                    description: WorkerLineage enables the lineage worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-monitor:
                    description: WorkerMonitor enables the monitor worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-portal:
                    description: WorkerPortal enables the worker portal
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-singletons:
                    description: WorkerSingletons enables the singletons worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker-temporal:
                    description: WorkerTemporal enables the temporal worker
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker2:
                    description: Worker2 enables the second worker instance
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                  worker3:
                    description: Worker3 enables the third worker instance
                    properties:
                      enabled:
                        description: Enabled specifies whether this component should
                          be installed
                        type: boolean
                      rawValues:
                        description: |-
                          RawValues contains arbitrary Helm values to pass directly to this component's subchart
                          These values will be merged with the component's default values and can override any
                          configuration option supported by the component's Helm chart
                        x-kubernetes-preserve-unknown-fields: true
                      replicas:
                        description: Replicas specifies the number of replicas for
                          this component
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies the resource requirements
                          for this component
                        properties:
                          limits:
                            description: Limits specifies the maximum resources that
                              can be used
                            properties:
                              cpu:
                                description: CPU specifies the maximum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the maximum memory resources
                                type: string
                            type: object
                          requests:
                            description: Requests specifies the minimum resources
                              that should be allocated
                            properties:
                              cpu:
                                description: CPU specifies the minimum CPU resources
                                type: string
                              memory:
                                description: Memory specifies the minimum memory resources
                                type: string
                            type: object
                        type: object
                    type: object
                type: object
              customVars:
                description: CustomVars contains custom environment variables to be
                  applied to the deployment
                items:
                  description: CustomVar defines a custom environment variable
                  properties:
                    name:
                      description: Name is the name of the environment variable
                      type: string
                    value:
                      description: Value is the value of the environment variable
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              email:
                description: Email contains configuration for email server settings
                properties:
                  defaultSender:
                    description: DefaultSender specifies the default sender email
                      address
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      SMTP password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  server:
                    description: Server specifies the SMTP server address
                    type: string
                  username:
                    description: Username specifies the SMTP username
                    type: string
                required:
                - defaultSender
                - server
                - username
                type: object
              freshpaint:
                description: Freshpaint contains configuration for Freshpaint analytics
                  integration
                properties:
                  avoToken:
                    description: AvoToken is the token for Avo analytics
                    type: string
                  backendToken:
                    description: BackendToken is the token for backend analytics
                    type: string
                  frontendToken:
                    description: FrontendToken is the token for frontend analytics
                    type: string
                  url:
                    description: URL is the Freshpaint project URL
                    type: string
                type: object
              global:
                description: Global contains the core configuration for the Datafold
                  deployment
                properties:
                  clusterName:
                    description: ClusterName specifies the name of the Kubernetes
                      cluster
                    type: string
                  deployment:
                    description: Deployment specifies the deployment name
                    type: string
                  environment:
                    description: Environment specifies the deployment environment
                      (e.g., production, staging, development)
                    type: string
                  githubServer:
                    description: GithubServer is the optional GitHub server URL for
                      GitHub integration
                    type: string
                  portalCertData:
                    description: PortalCertData is the optional certificate data to
                      verify which portal we're connecting to
                    type: string
                  portalUrl:
                    description: PortalUrl is the optional URL to override the default
                      portal endpoint
                    type: string
                  serverName:
                    description: ServerName is the domain name for the Datafold server
                    type: string
                required:
                - serverName
                type: object
              ingress:
                description: Ingress contains configuration for ingress and load balancer
                  setup
                properties:
                  aws:
                    description: AWS contains Amazon Web Services specific configuration
                    properties:
                      ingressGroupName:
                        description: |-
                          IngressGroupName specifies the name of the ingress group for shared load balancers
                          Multiple ingress resources with the same group name will share the same load balancer
                        type: string
                      ingressGroupOrder:
                        description: |-
                          IngressGroupOrder specifies the order of this ingress within the group
                          Lower numbers have higher priority
                        format: int32
                        type: integer
                      securityGroups:
                        description: SecurityGroups specifies the security groups
                          to use for the load balancer
                        type: string
                      subnets:
                        description: Subnets specifies the subnets to use for the
                          load balancer
                        type: string
                      targetGroupArn:
                        description: |-
                          TargetGroupName specifies the name of the existing target group
                          This is used when DeployLoadBalancer is false to connect to an existing load balancer
                        type: string
                    type: object
                  azure:
                    description: Azure contains Microsoft Azure specific configuration
                    properties:
                      backendPoolName:
                        description: |-
                          BackendPoolName specifies the name of the existing backend pool
                          This is used when DeployLoadBalancer is false to connect to an existing load balancer
                        type: string
                    type: object
                  deployLoadBalancer:
                    description: |-
                      DeployLoadBalancer specifies whether to deploy a complete load balancer
                      If false, the operator will only configure the ingress to connect to an existing load balancer
                    type: boolean
                  google:
                    description: Google contains Google Cloud Platform specific configuration
                    properties:
                      certificateMethod:
                        default: managed
                        description: CertificateMethod specifies the method the certificate
                          is attached to the load balancer
                        enum:
                        - managed
                        - preshared
                        - secret
                        type: string
                      cloudArmorPolicy:
                        description: CloudArmorPolicy allows the use of a Cloud Armor
                          policy, externally created.
                        type: string
                      networkEndpointGroupName:
                        description: |-
                          NetworkEndpointGroupName specifies the name of the existing Network Endpoint Group
                          This is used when DeployLoadBalancer is false to connect to an existing load balancer
                        type: string
                      presharedCertificate:
                        description: PresharedCertificate specifies the domain name
                          of the preshared certificate in the cloud
                        type: string
                      sslPolicy:
                        description: SslPolicy specifies an SSL policy to use.
                        type: string
                      staticIpName:
                        description: StaticIpName specifies the name of the static
                          IP address
                        type: string
                      tlsSecretName:
                        description: TlsSecretName specifies the name of a custom
                          made secret with cert and key
                        type: string
                    type: object
                  internal:
                    description: Internal specifies whether the load balancer is internal
                    type: boolean
                  sslCertificate:
                    description: SSLCertificate specifies the name of the SSL certificate
                    type: string
                type: object
              kept:
                description: KEPT contains configuration for Kept Graph Database Server
                properties:
                  port:
                    default: 8003
                    description: Port specifies the port for Kept server
                    format: int32
                    type: integer
                type: object
              maintenanceWindow:
                description: MaintenanceWindow contains configuration for the maintenance
                  window
                properties:
                  days:
                    description: |-
                      Days specifies which days this maintenance window applies to
                      Each string must be one of: monday, tuesday, wednesday, thursday, friday, saturday, sunday, daily
                    items:
                      description: MaintenanceDay represents a day of the week or
                        daily
                      enum:
                      - monday
                      - tuesday
                      - wednesday
                      - thursday
                      - friday
                      - saturday
                      - sunday
                      - daily
                      type: string
                    minItems: 1
                    type: array
                  end:
                    description: |-
                      End time in HH:MM format (24-hour format, UTC)
                      Can span midnight (e.g., "22:00" to "02:00")
                    type: string
                  start:
                    description: Start time in HH:MM format (24-hour format, UTC)
                    type: string
                required:
                - days
                - end
                - start
                type: object
              monitoring:
                description: Monitoring contains configuration for monitoring and
                  observability
                properties:
                  datadog:
                    description: Datadog contains Datadog-specific configuration
                    properties:
                      apm:
                        default: false
                        description: APM enables Application Performance Monitoring
                        type: boolean
                      applicationId:
                        description: ApplicationId contains the Datadog application
                          ID
                        type: string
                      clientToken:
                        description: ClientToken contains the Datadog client token
                        type: string
                      monitorPostgres:
                        default: true
                        description: MonitorPostgres enables PostgreSQL monitoring
                        type: boolean
                      npm:
                        description: NPM contains Network Performance Monitoring configuration
                        properties:
                          dnsstats:
                            default: true
                            description: Dnsstats enables DNS statistics collection
                            type: boolean
                          enabled:
                            default: true
                            description: Enabled enables Network Performance Monitoring
                            type: boolean
                        required:
                        - dnsstats
                        - enabled
                        type: object
                    required:
                    - apm
                    - monitorPostgres
                    - npm
                    type: object
                  monitoringApiKey:
                    default:
                      keyName: monitoringApiKey
                      secretName: datafold-operator-secrets
                    description: MonitoringApiKeySecret references the secret containing
                      the monitoring API key
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  type:
                    default: none
                    description: Type specifies the monitoring solution to use
                    enum:
                    - none
                    - datadog
                    type: string
                required:
                - monitoringApiKey
                type: object
              nginx:
                description: Nginx contains configuration for the nginx component
                properties:
                  loadBalancerIps:
                    description: LoadBalancerIps specifies the list of load balancer
                      IPs
                    items:
                      type: string
                    type: array
                  vpcCidrBlock:
                    default: 10.0.0.0/16
                    description: VpcCidrBlock specifies the CIDR block of the VPC
                    type: string
                required:
                - loadBalancerIps
                type: object
              operator:
                description: Operator contains configuration for the operator component
                properties:
                  version:
                    default: 1.2.8
                    description: Version specifies the version/tag of the operator
                      image
                    type: string
                type: object
              postgres:
                description: Postgres contains configuration for PostgreSQL database
                properties:
                  database:
                    description: Database specifies the database name
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      PostgreSQL password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  port:
                    default: 5432
                    description: Port specifies the port for PostgreSQL server
                    format: int32
                    type: integer
                  postgresMonitoringPasswordSecret:
                    description: PostgresMonitoringPasswordSecret references the secret
                      containing the Datadog monitoring password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  ro_user:
                    default: datafold_ro
                    description: ROUser specifies the read-only user
                    type: string
                  roPasswordSecret:
                    description: ROPasswordSecret references the secret containing
                      the read-only user password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  server:
                    description: Server specifies the PostgreSQL server address
                    type: string
                  user:
                    description: User specifies the PostgreSQL user
                    type: string
                type: object
              redis:
                description: Redis contains configuration for Redis cache
                properties:
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      Redis password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  port:
                    default: 6379
                    description: Port specifies the port for Redis server
                    format: int32
                    type: integer
                type: object
              secrets:
                description: Secrets contains references to customer-managed secrets
                properties:
                  apiKey:
                    default:
                      keyName: apiKey
                      secretName: datafold-operator-secrets
                    description: ApiKeySecret references the secret containing the
                      API key for portal authentication
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  claudeApiKey:
                    description: ClaudeApiKeySecret references the secret containing
                      the Claude API key for DMA containers
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  claudeDmaDocsKey:
                    description: ClaudeDmaDocsKeySecret references the secret containing
                      the Claude DMA docs authentication key
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  databaseEncryptionKey:
                    default:
                      keyName: databaseEncryptionKey
                      secretName: datafold-operator-secrets
                    description: DatabaseEncryptionKeySecret references the secret
                      containing the database encryption key
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  installPassword:
                    default:
                      keyName: installPassword
                      secretName: datafold-operator-secrets
                    description: InstallPasswordSecret references the secret containing
                      the initial admin password
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                  pylonId:
                    default:
                      keyName: pylonId
                      secretName: datafold-operator-secrets
                    description: PylonIdSecret references the secret containing the
                      pylon ID
                    properties:
                      keyName:
                        description: KeyName is the key within the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - keyName
                    - secretName
                    type: object
                required:
                - apiKey
                - databaseEncryptionKey
                - installPassword
                - pylonId
                type: object
              values:
                additionalProperties:
                  type: string
                description: Values contains custom Helm values to override defaults
                type: object
            required:
            - global
            - ingress
            - secrets
            type: object
          status:
            description: DatafoldApplicationStatus defines the observed state of DatafoldApplication.
            properties:
              components:
                additionalProperties:
                  description: ComponentStatus represents the status of a component
                  properties:
                    lastUpdateTime:
                      description: LastUpdateTime is the last time the status was
                        updated
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about the
                        component status
                      type: string
                    phase:
                      allOf:
                      - enum:
                        - Running
                        - Failed
                        - Pending
                        - Unknown
                        - Disabled
                      - enum:
                        - Running
                        - Failed
                        - Pending
                        - Unknown
                        - Disabled
                      description: Phase represents the current phase of the component
                      type: string
                  type: object
                description: Components tracks the status of individual components
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the application's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              healthCheckAttempts:
                default: 0
                description: |-
                  HealthCheckAttempts tracks the number of attempts made for the current health check trigger
                  This is reset when a new trigger value is detected
                format: int32
                type: integer
              lastAppliedValues:
                additionalProperties:
                  type: string
                description: LastAppliedValues stores the last applied Helm values
                type: object
              lastHealthCheckTrigger:
                description: |-
                  LastHealthCheckTrigger records the trigger value of the last health check that was performed
                  This serves as both the trigger value and timestamp for when the last health check was run
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the observed generation of
                  the spec
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the deployment
                enum:
                - Pending
                - Installing
                - Running
                - Failed
                - Updating
                type: string
              services:
                description: Services tracks the connectivity status of external services
                items:
                  description: ServiceStatus represents the connectivity status of
                    an external service
                  properties:
                    lastCheckTime:
                      description: LastCheckTime is the last time the connectivity
                        was checked
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about the
                        service connectivity
                      type: string
                    name:
                      description: Name is the name of the service (e.g., "redis",
                        "postgres", "clickhouse")
                      type: string
                    status:
                      allOf:
                      - enum:
                        - Reachable
                        - Unreachable
                        - Unknown
                      - enum:
                        - Reachable
                        - Unreachable
                        - Unknown
                      description: Status represents the connectivity status of the
                        service
                      type: string
                    storageLastCheckTime:
                      description: StorageLastCheckTime is the last time the storage
                        availability was checked
                      format: date-time
                      type: string
                    storageMessage:
                      description: StorageMessage provides additional information
                        about the storage availability
                      type: string
                    storageStatus:
                      description: StorageStatus represents the storage availability
                        status (for services that support storage checks)
                      enum:
                      - Sufficient
                      - Insufficient
                      - Unknown
                      type: string
                    tableValidationLastCheckTime:
                      description: TableValidationLastCheckTime is the last time the
                        table validation was checked
                      format: date-time
                      type: string
                    tableValidationMessage:
                      description: TableValidationMessage provides additional information
                        about the table validation
                      type: string
                    tableValidationStatus:
                      description: TableValidationStatus represents the table validation
                        status (for services that support table validation)
                      enum:
                      - TablesPresent
                      - TablesNotPresent
                      - Unknown
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
            required:
            - healthCheckAttempts
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
